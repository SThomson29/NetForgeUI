# Development and testing dependencies
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Jinja2 for template smoke tests (also used by Ansible)
jinja2>=3.1.0
