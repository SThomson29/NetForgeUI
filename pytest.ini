[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    slow: marks tests as slow (e.g. full ansible runs)
    unit: pure unit tests with no I/O
    integration: Flask route tests
    generation: config generation tests requiring Ansible
